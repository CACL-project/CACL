# Pinned dependencies for CI determinism.
# Used by GitHub Actions and Docker tests.

# Core library dependencies
fastapi==0.115.6
sqlalchemy==2.0.36
python-jose==3.3.0
python-dotenv==1.0.1

# Transitive deps pinned for determinism (pulled by fastapi)
starlette==0.41.3
pydantic==2.10.4

# PostgreSQL driver
asyncpg==0.30.0
greenlet==3.1.1

# Test framework
pytest==8.3.4
pytest-asyncio==0.24.0
